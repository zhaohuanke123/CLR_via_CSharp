namespace TestActionLambda
{
    class Program
    {
        static void Main(string[] args)
        {
            // Action<int> action = null;
            // action += (int x) => Console.WriteLine(x);
            // var tmp = action;
            // action -= (int x) => Console.WriteLine(x);
            //
            // action.Invoke(1);
            // Console.WriteLine(ReferenceEquals(action, tmp));
        }
    }
}